import "./App.css";

import Home from "./Home";

const App = () => {
  //const endpoint = useMemo(() => clusterApiUrl(network), []);

  return (
              <Home
              />
  );
};

export default App;
